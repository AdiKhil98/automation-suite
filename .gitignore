# Environment / secrets
.env
.env.*
!.env.example

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/

# Playwright / capture artifacts (private screenshots — never committed)
playwright-report/
test-results/
/screenshots/
/artifacts/
.artifacts/

# Audit paid-result recovery envelopes (local, private — never committed)
.audit-tmp/
# Audit validation debug envelopes (local, private — never committed)
.audit-debug/
# Eval matrix reports (local)
eval-reports/

# Local database volumes / dumps
/.postgres-data/
*.sql.bak
*.dump

# Logs
*.log
logs/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
